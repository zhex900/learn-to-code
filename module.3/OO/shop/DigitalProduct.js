/**
 * 
 * Define a class named DigitalProduct that extends Product.

The constructor must accept name, price, and downloadLink.

Override the displayInfo() method to also log the specific 
downloadLink (since it's important information for a digital item).
 */
