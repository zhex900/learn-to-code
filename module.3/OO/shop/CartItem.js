/**
 *
 * Define a class named CartItem.
 * private property id.
 * The constructor should accept productName, price, and quantity.
 * Add getId method
 * Add a method called getTotalPrice() that calculates and
 * returns the item's total cost ($\text{price} \times \text{quantity}$).
 *
 */
